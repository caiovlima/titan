services:
  titan-web:
    build: .
    ports:
      - '4000:4000'
    environment:
      - NODE_ENV=production
  nginx:
    image: nginx:alpine
    ports:
      - '8080:80'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - titan-web
